# Raw downloads — too large for git
data/raw/

# Large sequence files — not needed at inference time
data/processed/sequences_train.pkl

# Trained model weights — commit these after training locally, then push
# artifacts/*.pt   ← uncomment this line to re-exclude weights if repo gets too large

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Streamlit cache (not the config — that gets committed)
.streamlit/cache/
